# Image Pull Secrets with `data.kubernetes_namespace`

This example demonstrates how to create an image pull secret in an existing Kubernetes namespace that is not managed by Terraform. This includes default namespaces (`default`, `kube-system`, etc.).
